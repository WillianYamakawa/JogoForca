{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "(gdb) Launch",
            "type": "cppdbg",
            "request": "launch",
            "args": ["-a", "${workspaceFolder}/build/palavras.txt"],
            "program": "${workspaceFolder}/build/HangMan.exe",
            "cwd": "${workspaceFolder}",
            "stopAtEntry": false,
            "preLaunchTask": "C/C++: gcc.exe build active file",
        }
    ]
}